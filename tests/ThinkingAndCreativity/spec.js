let functionInput = [true, false /*... length = 75 */];

let functionResult = { // value = level in levels [low, medium, high]
    objective: 'high', // key = key of thinking type
    creativity: 'low' // key = 'creativity' always
};
